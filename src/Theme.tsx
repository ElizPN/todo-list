import { createTheme } from "@mui/material/styles";

export const Theme = {
  palette: {
    primary: {
      main: "#3f50b5",
    },
    secondary: {
      main: "#388e3c",
    },
  },
};

export const theme = createTheme(Theme);
